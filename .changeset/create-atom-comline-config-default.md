---
"create-atom.io": patch
---

Upgrade comline to 0.8.0 and remove the redundant config-discovery stub now that config loading is opt-in. The CLI continues to ignore config files and accepts options through flags or interactive prompts.
