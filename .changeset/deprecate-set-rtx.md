---
"atom.io": patch
---

Mark `SetRTX` as deprecated. Its byte-level wire encoding tests moved out of the public contract, while `OList` and `UList` remain the supported mutable collection transceivers.
