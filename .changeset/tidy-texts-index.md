---
"atom.io": patch
---

Add a bounded-fanout Mosaic text index with stable run-relative fragments, logarithmic Unicode offset, grapheme, line, and range lookup, hysteretic split and merge maintenance, bounded stale-leaf aliases, structured range resnapshot recovery, partial-residency adapters, deterministic maintenance counters, and seeded multi-client convergence coverage.
