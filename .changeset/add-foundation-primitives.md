---
"atom.io": patch
---

Add foundation submodules `atom.io/foundations/future` and `atom.io/foundations/subject`. `Future`, `Subject`, and `StatefulSubject` now live in those public homes and are no longer exported from `atom.io/internal`.
