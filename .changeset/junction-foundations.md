---
"atom.io": minor
---

Add a public foundation submodule for Junction.

`Junction` and its related types are now available from `atom.io/foundations/junction`. These APIs are no longer exported from `atom.io/internal`.
