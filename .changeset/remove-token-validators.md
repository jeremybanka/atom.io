---
"atom.io": minor
---

Remove the unused token validator exports. Token `type` fields are now represented directly in the public token type definitions instead of through runtime validator helpers.
