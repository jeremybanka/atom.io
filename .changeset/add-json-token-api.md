---
"atom.io": patch
---

Add `getJsonToken` for mutable atoms and mutable atom family members. It returns the writable selector token representing the mutable state's JSON form, matching `useJson` behavior in the core API.
