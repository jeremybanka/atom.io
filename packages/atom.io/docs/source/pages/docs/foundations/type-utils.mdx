import FlatExample from "x/foundations/type-utils/flat.gen";
import ViewOfExample from "x/foundations/type-utils/view-of.gen";

# <span className="soft">atom.io</span>/foundations/type-utils

Use `atom.io/foundations/type-utils` for small shared type helpers.

Import `Flat`, `ViewOf`, or both from `atom.io/foundations/type-utils`.

## package contents

<table>
  <tbody>
    <tr>
      <td>
        <code>Flat</code>
      </td>
      <td>Flatten a mapped object type into a readable object shape.</td>
    </tr>
    <tr>
      <td>
        <code>ViewOf</code>
      </td>
      <td>Derive the readonly view type of arrays, maps, sets, and transceivers.</td>
    </tr>
  </tbody>
</table>

## Flat

`Flat` is mostly for type display and type composition.

<FlatExample client:load />

## ViewOf

`ViewOf` converts common mutable containers into readonly views. If a type declares a
`READONLY_VIEW` marker, `ViewOf` uses that marker.

<ViewOfExample client:load />
