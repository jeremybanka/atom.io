---
"create-atom.io": patch
---

Honor a preselected template when creating a project, skipping the template prompt and scaffolding the requested template. Prompt for a project directory only when no directory argument is supplied.
