# <span className="soft">atom.io</span>/foundations

Foundations are small public modules that support atom.io's core APIs without being atom.io state features themselves.

Use them when you need the same low-level value, collection, and type utilities that atom.io uses internally.

## package contents

<table>
  <tbody>
    <tr>
      <td>
        <code>atom.io/foundations/json</code>
      </td>
      <td>
        JSON-safe value types, typed parse/stringify helpers, JSON type names,
        default JSON values, and <code>isJson</code>.
      </td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/canonical</code>
      </td>
      <td>
        Canonical key types and compact packing helpers for stable primitive and
        tuple identity.
      </td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/entries</code>
      </td>
      <td>
        Typed wrappers for <code>Object.entries</code> and{" "}
        <code>Object.fromEntries</code>.
      </td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/enumeration</code>
      </td>
      <td>Create a two-way enum-like object from a readonly string array.</td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/type-utils</code>
      </td>
      <td>Small reusable type helpers such as <code>Flat</code> and <code>ViewOf</code>.</td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/future</code>
      </td>
      <td>A small imperative future primitive.</td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/subject</code>
      </td>
      <td>Observable subject primitives.</td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/overlays</code>
      </td>
      <td>Overlay collection classes for staging Map and Set changes over a source.</td>
    </tr>
    <tr>
      <td>
        <code>atom.io/foundations/junction</code>
      </td>
      <td>A bidirectional relation helper for two-sided string-keyed associations.</td>
    </tr>
  </tbody>
</table>

## serializable json

Use `Json.Serializable` from `atom.io/foundations/json` when a value needs to cross a serialization boundary.

That includes state sent across a network, values stored in persistence, and messages shared between different ECMAScript environments.

## canonical keys

Use `Canonical` from `atom.io/foundations/canonical` when you need a structured key with predictable identity.

Objects are not a great fit for this. Their key order is not always the identity you mean to express, and TypeScript's duck typing can allow extra keys to sneak in.

Tuples are usually a better shape for compound keys.

## typed entries

Use `toEntries` and `fromEntries` from `atom.io/foundations/entries` when you want the shape of an object to survive a trip through `Object.entries` or `Object.fromEntries`.

## enumerations

Use `enumeration` from `atom.io/foundations/enumeration` when you want a small two-way mapping from strings to indexes and indexes back to strings.
