import Exhibit from "c/Exhibit.astro";

# <low-emphasis>atom.io</low-emphasis>/realtime-server

`atom.io/realtime-server` hosts authoritative state transitions. Its public
surfaces are transport-neutral where possible, with Socket.IO binders for the
standard protocol paths.

The server is responsible for authentication, authorization, validation,
serialization, durable ordering, bounded recovery, cleanup, and persistence.
The package supplies contracts and in-memory reference adapters; your
deployment supplies verified identity and durable storage.

## package contents

Every public export from `atom.io/realtime-server` is listed below. Classes that
also name their instance type appear in both runtime and contract columns.

<table-wrapper>
	<table>
		<thead>
			<tr>
				<th>Area</th>
				<th>Runtime exports</th>
				<th>Associated contracts</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td>Connection entrypoint</td>
				<td><code>realtime</code></td>
				<td>
					<code>Handshake</code>, <code>ServerConfig</code>, <code>UserServerConfig</code>
				</td>
			</tr>
			<tr>
				<td>Rigid providers</td>
				<td>
					<code>realtimeAtomFamilyProvider</code>, <code>realtimeMutableFamilyProvider</code>, <code>realtimeMutableProvider</code>, <code>realtimeStateProvider</code>, <code>realtimeStateReceiver</code>
				</td>
				<td>
					<code>FamilyProvider</code>, <code>MutableFamilyProvider</code>, <code>MutableProvider</code>, <code>RealtimeLeaseClock</code>, <code>RealtimeStateReceiverOptions</code>, <code>StateProvider</code>, <code>StateReceiver</code>
				</td>
			</tr>
			<tr>
				<td>Socket state</td>
				<td>
					<code>onlineUsersAtom</code>, <code>socketAtoms</code>, <code>socketKeysAtom</code>, <code>usersOfSockets</code>
				</td>
				<td><code>SocketSystemHierarchy</code></td>
			</tr>
			<tr>
				<td>Rooms</td>
				<td>
					<code>DEFAULT_ROOM_TIMEOUTS</code>, <code>ROOMS</code>, <code>destroyRoom</code>, <code>provideEnterAndExit</code>, <code>provideRooms</code>, <code>roomMeta</code>, <code>spawnRoom</code>
				</td>
				<td>
					<code>DestroyRoomConfig</code>, <code>ProvideEnterAndExitConfig</code>, <code>ProvideRoomsConfig</code>, <code>RoomMap</code>, <code>RoomProcessFactory</code>, <code>RoomTimeouts</code>, <code>SpawnRoomConfig</code>
				</td>
			</tr>
			<tr>
				<td>IPC child socket</td>
				<td><code>ChildSocket</code></td>
				<td>
					<code>ChildProcess</code>, <code>ChildSocket</code>, <code>StderrLog</code>
				</td>
			</tr>
			<tr>
				<td>IPC custom socket</td>
				<td><code>CustomSocket</code>, <code>isEventPayload</code></td>
				<td>
					<code>CustomSocket</code>, <code>EventBuffer</code>, <code>EventPayload</code>, <code>Events</code>
				</td>
			</tr>
			<tr>
				<td>IPC framing</td>
				<td>
					<code>DelimitedJsonCodec</code>, <code>IPC_FRAME_DELIMITER</code>, <code>encodeJsonFrame</code>
				</td>
				<td>
					<code>DelimitedJsonCodec</code>, <code>DelimitedJsonCodecOptions</code>
				</td>
			</tr>
			<tr>
				<td>IPC parent socket</td>
				<td>
					<code>PROOF_OF_LIFE_SIGNAL</code>, <code>ParentSocket</code>, <code>SubjectSocket</code>
				</td>
				<td>
					<code>ParentProcess</code>, <code>ParentSocket</code>, <code>SubjectSocket</code>
				</td>
			</tr>
			<tr>
				<td>Atomic Mosaic server</td>
				<td>
					<code>createMosaicServer</code>, <code>defineMosaicAtomRegistration</code>, <code>fingerprintMosaicOperation</code>
				</td>
				<td>
					<code>MosaicAtomRegistration</code>, <code>MosaicAuthorizationAction</code>, <code>MosaicAuthorizationContext</code>, <code>MosaicPresenceContext</code>, <code>MosaicServer</code>, <code>MosaicServerAtomStatus</code>, <code>MosaicServerConnection</code>, <code>MosaicServerOptions</code>, <code>MosaicServerSnapshot</code>, <code>MosaicServerTarget</code>, <code>MosaicTransceiverClass</code>
				</td>
			</tr>
			<tr>
				<td>Atomic Mosaic storage</td>
				<td><code>InMemoryMosaicStorage</code></td>
				<td>
					<code>InMemoryMosaicStorage</code>, <code>MosaicHeadHint</code>, <code>MosaicStorageAdapter</code>, <code>MosaicStorageAppendRequest</code>, <code>MosaicStorageAppendResult</code>, <code>MosaicStorageCheckpointRequest</code>, <code>MosaicStorageCheckpointResult</code>, <code>MosaicStorageRecovery</code>, <code>MosaicStorageResult</code>, <code>MosaicStoredCheckpoint</code>, <code>MosaicStoredReceipt</code>
				</td>
			</tr>
			<tr>
				<td>Domain batch authority</td>
				<td>
					<code>createMosaicDomainBatchServer</code>, <code>fingerprintMosaicDomainBatch</code>
				</td>
				<td>
					<code>MosaicDomainBatchAuthorizationContext</code>, <code>MosaicDomainBatchConnection</code>, <code>MosaicDomainBatchProposalResult</code>, <code>MosaicDomainBatchServer</code>, <code>MosaicDomainBatchServerOptions</code>
				</td>
			</tr>
			<tr>
				<td>Domain batch storage</td>
				<td><code>InMemoryMosaicDomainBatchStorage</code></td>
				<td>
					<code>InMemoryMosaicDomainBatchStorage</code>, <code>MosaicDomainBatchAppendRequest</code>, <code>MosaicDomainBatchAppendResult</code>, <code>MosaicDomainBatchReceipt</code>, <code>MosaicDomainBatchRecovery</code>, <code>MosaicDomainBatchStorageAdapter</code>, <code>MosaicDomainBatchStorageResult</code>
				</td>
			</tr>
			<tr>
				<td>Checkpoint coordinator</td>
				<td>
					<code>createMosaicDomainCheckpointCoordinator</code>, <code>stageMosaicDomainExternalCheckpointGraph</code>
				</td>
				<td>
					<code>MosaicDomainCheckpointCoordinator</code>, <code>MosaicDomainCheckpointCoordinatorOptions</code>, <code>MosaicDomainCheckpointIndexUpdate</code>, <code>MosaicDomainCheckpointResult</code>, <code>MosaicDomainExternalCheckpointGraphLimits</code>, <code>MosaicDomainExternalCheckpointGraphOptions</code>, <code>MosaicDomainExternalCheckpointGraphResult</code>, <code>MosaicDomainExternalCheckpointGraphUpdate</code>, <code>MosaicDomainExternalCheckpointReader</code>
				</td>
			</tr>
			<tr>
				<td>Checkpoint storage</td>
				<td>
					<code>InMemoryMosaicDomainCheckpointStorage</code>, <code>mosaicDomainCheckpointObjectKey</code>
				</td>
				<td>
					<code>InMemoryMosaicDomainCheckpointStorage</code>, <code>MosaicDomainCheckpointAcceptedProposal</code>, <code>MosaicDomainCheckpointBatchAppendRequest</code>, <code>MosaicDomainCheckpointCollectionRequest</code>, <code>MosaicDomainCheckpointCollectionResult</code>, <code>MosaicDomainCheckpointCommitRequest</code>, <code>MosaicDomainCheckpointCommitResult</code>, <code>MosaicDomainCheckpointExternalGraphProof</code>, <code>MosaicDomainCheckpointHead</code>, <code>MosaicDomainCheckpointObjectPage</code>, <code>MosaicDomainCheckpointStageOptions</code>, <code>MosaicDomainCheckpointStageProposal</code>, <code>MosaicDomainCheckpointStageResult</code>, <code>MosaicDomainCheckpointStorageAdapter</code>, <code>MosaicDomainCheckpointStoredObject</code>
				</td>
			</tr>
			<tr>
				<td>Domain history</td>
				<td>
					<code>DEFAULT_MOSAIC_DOMAIN_HISTORY_LIMITS</code>, <code>bindMosaicDomainHistoryServerSocket</code>, <code>createMosaicDomainHistoryCoordinator</code>
				</td>
				<td>
					<code>MosaicDomainHistoryCheckpointIntegration</code>, <code>MosaicDomainHistoryConnection</code>, <code>MosaicDomainHistoryCoordinator</code>, <code>MosaicDomainHistoryCoordinatorOptions</code>, <code>MosaicDomainHistoryLimits</code>, <code>MosaicDomainHistoryServerSocket</code>, <code>MosaicDomainHistoryStats</code>, <code>MosaicDomainHistoryWorkTracker</code>
				</td>
			</tr>
			<tr>
				<td>Domain presence</td>
				<td>
					<code>bindMosaicDomainPresenceServerSocket</code>, <code>createMosaicDomainPresenceServer</code>
				</td>
				<td>
					<code>MosaicDomainPresenceConnection</code>, <code>MosaicDomainPresenceServer</code>, <code>MosaicDomainPresenceServerOptions</code>, <code>MosaicDomainPresenceServerSocket</code>, <code>MosaicDomainPresenceWorkTracker</code>
				</td>
			</tr>
			<tr>
				<td>Domain residency</td>
				<td>
					<code>bindMosaicDomainResidencyServerSocket</code>, <code>createMosaicDomainResidencyServer</code>
				</td>
				<td>
					<code>MosaicDomainRangeResolver</code>, <code>MosaicDomainResidencyAuthorizationContext</code>, <code>MosaicDomainResidencyServer</code>, <code>MosaicDomainResidencyServerOptions</code>, <code>MosaicDomainResidencyServerSocket</code>, <code>MosaicDomainResidencyWorkTracker</code>
				</td>
			</tr>
			<tr>
				<td>Text document coordination</td>
				<td>
					<code>createMosaicTextDocumentCoordinator</code>, <code>prepareMosaicTextImportOperation</code>
				</td>
				<td>
					<code>MosaicTextDocumentCommand</code>, <code>MosaicTextDocumentCoordinator</code>, <code>MosaicTextDocumentInstrumentation</code>, <code>MosaicTextDocumentRangeInspection</code>
				</td>
			</tr>
			<tr>
				<td>Content-addressed text storage</td>
				<td>
					<code>MOSAIC_TEXT_ROOT_CHECKPOINT_INDEX</code>, <code>MOSAIC_TEXT_ROOT_MANIFEST_INDEX</code>, <code>MOSAIC_TEXT_ROOT_REFERENCE_COUNT_INDEX</code>, <code>createMosaicTextRootCheckpointReader</code>, <code>createMosaicTextRootCheckpointStage</code>
				</td>
				<td>
					<code>MosaicTextRootCheckpointReader</code>, <code>MosaicTextRootCheckpointStage</code>, <code>MosaicTextRootCheckpointStageOptions</code>, <code>MosaicTextRootExternalIndexReader</code>
				</td>
			</tr>
		</tbody>
	</table>
</table-wrapper>

## authenticate before composition

The rigid `realtime()` helper authenticates a Socket.IO handshake before it
creates user state or invokes the connection callback. Domain binders should be
given actor and session identity from the authenticated connection, not accept
those identities from a proposal body.

Authorization is still required after authentication. Check action, actor,
session, Domain instance, member/key, operation meaning, and product role before
acceptance. A user who may read a design does not necessarily get to edit every
glyph or publish arbitrary presence.

## server component map

<table-wrapper>
	<table>
		<thead>
			<tr>
				<th>Component</th>
				<th>Responsibility</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td><code>createMosaicServer</code></td>
				<td>
					Atomic Mosaic registration, join, reduce, recovery, presence, and checkpoints for individual mutable atoms.
				</td>
			</tr>
			<tr>
				<td><code>createMosaicDomainBatchServer</code></td>
				<td>
					Normalize, authorize, serialize, append, and broadcast atomic Domain batches.
				</td>
			</tr>
			<tr>
				<td><code>createMosaicDomainResidencyServer</code></td>
				<td>
					Authorize explicit/range selections and deliver filtered snapshots and tails.
				</td>
			</tr>
			<tr>
				<td><code>createMosaicTextDocumentCoordinator</code></td>
				<td>
					Stage/import content-addressed text, translate commands into atomic source/index batches, inspect bounded ranges, and expose measured work.
				</td>
			</tr>
			<tr>
				<td><code>createMosaicDomainPresenceServer</code></td>
				<td>
					Bind actor/session presence, rate limit, snapshot, expire, and clean up.
				</td>
			</tr>
			<tr>
				<td><code>createMosaicDomainHistoryCoordinator</code></td>
				<td>
					Maintain actor cursors/horizons and submit validated compensating batches.
				</td>
			</tr>
			<tr>
				<td><code>createMosaicDomainCheckpointCoordinator</code></td>
				<td>
					Publish fenced content-addressed roots, recover selections, and coordinate retention/GC.
				</td>
			</tr>
			<tr>
				<td><code>realtimeStateProvider</code></td>
				<td>Serve rigid snapshots and subscriptions.</td>
			</tr>
			<tr>
				<td><code>realtimeStateReceiver</code></td>
				<td>Validate leased rigid publications and expire ownership.</td>
			</tr>
			<tr>
				<td><code>provideRooms</code></td>
				<td>Authorize and host isolated room processes with bounded lifecycle.</td>
			</tr>
		</tbody>
	</table>
</table-wrapper>

## batch authority

<Exhibit src="realtime/coordinate-a-domain-batch.ts" />

The batch server performs normalization exactly once at the public boundary and
uses the canonical result for authorization, storage, reduction, and broadcast.
Do not authorize one value and persist a caller-mutated or pre-normalized object.

Append is serialized. Duplicate ids are accepted only when their canonical
fingerprint matches. A collision fails closed. If another writer commits the
same batch between lookup and append, synchronize resident revision and
projection before acknowledging the duplicate.

## storage adapters

Production adapters implement the public batch and checkpoint storage
contracts. In-memory adapters are useful for tests and examples, not process
durability. Run the reusable conformance suites against every adapter:

- atomic append and stale-head behavior;
- exact duplicate versus id collision;
- recovery gaps and bounded tails;
- concurrent writers and restart;
- root/revision/retention fencing;
- content-key verification and pagination;
- retention leases and eventual garbage collection;
- partial failure that cannot publish a half checkpoint.

The checkpoint storage adapter may also be the batch storage adapter so one
revision stream and checkpoint root share an atomic backing implementation.

## checkpoints

<Exhibit src="realtime/checkpoint-a-domain-incrementally.ts" />

Object staging is harmless until a root is published. Root publication must
compare the expected stream revision, previous root, and retention epoch.
Checkpoint readers pin a root and page requested member/index paths rather than
loading every object.

External roots, such as a content-addressed Mosaic Text root, require an
accepted-root handoff: an accepted proposal protects its staged root across
restart until a Domain checkpoint adopts it. A checkpoint that omits an accepted
external root must be rejected.

## history authority

<Exhibit src="realtime/coordinate-domain-history.ts" />

Only the server coordinator may mint collaborative compensation capability.
Public batch clients cannot forge an operation and label it undo. Before append,
the coordinator revalidates the actor cursor, session sequence, horizon,
original targets, model classification, and any excluded maintenance appended
by `completeCompensation`.

History indexes and session watermarks must survive restart. Compact model
snapshots with the exact retained operation-id set supplied by the coordinator.

## residency and range resolvers

Residency authorization occurs before family allocation. A range resolver must
enforce its own schema, byte/depth/count bounds, and maximum output addresses.
Return only addresses belonging to the active Domain. When an index identity is
stale after split/merge, provide a structured resnapshot/recovery result rather
than guessing.

Filtered accepted envelopes retain bounded batch metadata so resident members
touched by one batch can settle atomically even when other batch members are not
loaded.

`bindMosaicDomainResidencyServerSocket` binds that authority to an
authenticated Socket.IO connection and tracks pending work for orderly
shutdown. The binder receives trusted actor/session identity from composition;
it does not accept identity from a residency request.

## text document coordination

`createMosaicTextDocumentCoordinator` is the application-facing server seam for
large collaborative text. It stages grapheme-safe imports, prepares atomic
logical-text and index operations, publishes accepted external roots through
the Domain checkpoint lifecycle, serves bounded range inspection, and reports
object, byte, scan, and persistence work. Consumers provide policy and storage;
they should not assemble a second sidecar text model.

The coordinator and external-root staging helpers are trusted server
composition APIs, not socket handlers. Authenticate and authorize the resource,
then enforce operation, object, byte, depth, rate, deadline, and cancellation
limits before constructing work from client-controlled input. The larger limits
needed for a manifest-authenticated bulk import are not safe defaults for an
ordinary interactive edit.

## presence cleanup

Presence servers enforce payload bytes, updates per second, pending updates,
sessions per actor, total sessions, and transport queues. Disconnect and expiry
must clear projections even when listeners throw. Session retirement is also a
hook for releasing associated residency or retention resources.

## deployment ownership

Atom.io does not choose your database, identity provider, admission policy,
replication topology, backups, or disaster-recovery objective. A production
host should define:

- an atomic transaction for batch append, receipts, watermarks, and checkpoint roots;
- authenticated actor/session derivation and revocation;
- per-tenant and per-resource quotas;
- retention and backup policy for immutable objects and roots;
- graceful shutdown that drains or rejects pending authoritative work;
- schema/model version rollout and rollback; and
- observable revisions, queue depth, rejection codes, checkpoint age, and GC progress.

Continue with [operations and security](/docs/realtime/operations) for the
release checklist.
