---
"atom.io": patch
---

Add a package export type declaration for `atom.io/react-devtools/css` so TypeScript consumers can import the React DevTools stylesheet without relying on a broad CSS module shim.
