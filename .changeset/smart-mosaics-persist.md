---
"atom.io": patch
---

Add the Mosaic server and linearizable persistence contract, with durable
idempotent receipts, revision-CAS append, checkpoint recovery, selective
history validation, multi-node head hints, and ephemeral presence.
