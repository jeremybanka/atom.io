import CompoundKey from "x/foundations/canonical/compound-key.gen";
import DistinctPrimitiveKeys from "x/foundations/canonical/distinct-primitive-keys.gen";

# <span className="soft">atom.io</span>/foundations/canonical

Use `atom.io/foundations/canonical` when a key needs stable identity across maps,
stores, URLs, or JSON boundaries.

Canonical values are primitives or readonly arrays of canonical values. Prefer tuples
for compound keys.

<CompoundKey client:load />

## package contents

<table>
  <tbody>
    <tr>
      <td>
        <code>Canonical</code>
      </td>
      <td>A primitive or readonly array of canonical values.</td>
    </tr>
    <tr>
      <td>
        <code>packed</code>
      </td>
      <td>A string branded with the canonical value it represents.</td>
    </tr>
    <tr>
      <td>
        <code>packCanonical</code>
      </td>
      <td>Convert a canonical value into a compact string key.</td>
    </tr>
    <tr>
      <td>
        <code>unpackCanonical</code>
      </td>
      <td>Recover a canonical value from a packed key.</td>
    </tr>
  </tbody>
</table>

## why not just JSON.stringify?

`packCanonical` keeps primitive types distinct before stringifying. That means values
that can look similar as plain strings remain different keys.

<DistinctPrimitiveKeys client:load />
